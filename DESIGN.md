# 介面設計規範

## 參考與範圍

使用者指定沿用 Quota-PromptDock，在現有程式上新增功能。基準為提交 `52cf27819691e7083e2ee647cfb144f32de502d5` 與原始 `widget-demo-new.png`。

保留深藍黑底、綠色額度圓環、膠囊標籤、圓角卡片、緊湊文字、Claude 琥珀色與系統匣操作。新增的文字按鈕使用原有卡片配色。保真目標是延續同一個產品，而非改成先前提議的白色 WPF 介面。

Codex 額度仍在上方，Claude 沿用原專案的卡片直接顯示，下方新增兩欄常用指令。依使用者標示移除明細按鈕及固定操作提示列；操作結果以短暫提示呈現。以既有原生 Qt 介面和截圖作為視覺基準，功能變更維護於 `feature/prompt-dock` 分支。

## 視覺值

| 項目 | 規範 |
|---|---|
| 主背景 | `#0B1220` |
| 卡片 | `#111C2E` |
| 主要操作／圓環 | `#35E28A` |
| 指令按鈕 | `#182438`，邊框 `#334155` |
| 主文字／次文字 | `#F8FAFC`／`#94A3B8` |
| 錯誤 | `#FCA5A5`，搭配文字說明 |
| 字型 | 沿用原專案，Windows 中文優先微軟正黑體 |
| 字級 | 標題 17px、操作 13–14px、輔助 11–12px |
| 間距 | 項目 8px、卡片內 12px、外框內約 22px |
| 圓角 | 外框 24px、卡片 14px、指令按鈕 10px |
| 尺寸 | 寬 400 邏輯像素，高度依內容收緊、上限 940；可用高度低於 850 時改用緊湊額度摘要 |

## 操作與狀態

- 指令預設顯示前 8 個，其餘由「更多指令」展開。長名稱省略顯示，滑鼠提示保留完整文字。
- 指令點擊後短暫停用，避免連點覆蓋剪貼簿；貼上前再次確認目標焦點及文字未被改動。
- 編輯視窗具有清單、名稱、全文、順序操作與儲存／取消。空白名稱或內容不能儲存；刪除前確認。檔案儲存成功才關閉，失敗時保留草稿、排序及欄位，顯示可重試的錯誤文字。
- 清單選取使用整列深綠底；鍵盤焦點使用同一列完整的 2px 綠色框。取消原生文字內框，外層清單保持淡灰邊框，避免雙重選取。次要操作聚焦使用 2px 淺色框；第一項上移、最後一項下移與空清單刪除停用。
- 有資料顯示額度、更新時間；無資料顯示文字；失敗保留舊數值並標記。空指令清單顯示新增入口。
- 所有新增按鈕和輸入欄位有鍵盤焦點樣式及可存取名稱；無新增動畫，不依賴動作效果理解操作。
- 額度區獨立捲動，8 個常用指令及底部操作保持可見。展開更多時只捲動指令清單，兩個捲動區域互為兄弟元件，沒有巢狀捲動。150% 空間不足時可向下捲動額度區查看完整 Claude 卡片；展開時重新確認視窗在工作區內。
- 首次查詢失敗以「未登入」、「未找到」或「更新失敗」取代「連線中」，並提供下一步；有舊資料時保留方案及額度，顯示最後成功時間。複製備援及操作失敗提示延長至 10 秒，沒有恢復固定提示列。
- 介面縮放提供 75%、90%、100%、110%、125%、150%，預設 100%。以 Qt 原生縮放同時調整文字、按鈕、圓環和對話框，變更後重新啟動並保存比例，不累乘上一次的應用程式縮放。
- 設定內容可捲動，儲存／取消固定於底部；編輯視窗尺寸依工作區限制。極大縮放下允許指令區捲動，避免操作超出畫面。
- 下拉欄位保留完整圓角外框，箭頭區透明且無獨立邊線，右側預留 30px。使用一致的向量下箭頭，避免原生箭頭按鈕的底色遮住欄位外框；原生下拉與鍵盤行為保留。
- Claude 卡片與常用指令標題維持 8px 邏輯間距。視窗依實際內容高度收緊，保留底部位置，不把多餘高度變成卡片下方的空白；內容超過工作區才捲動。

## Skill 套用

已在專案執行兩個指定初始化／安裝命令。UI UX Pro Max 用於焦點、對比、間距與回饋；Agentic Design System 的參考校準、設計審查、空白／載入／錯誤檢查與細節檢查用於原生 Qt 介面。

原生截圖已比對原專案，保留實心深色背景與原 Claude 卡片。網頁用的 DOM、瀏覽器 Lighthouse 等檢查不列為原生 App 驗收。真實 Windows 輸入與螢幕切換限制另見驗證紀錄。
