# 驗證紀錄

日期：2026-09-10。基礎：指定 GitHub 專案的 52cf278 提交。本次使用 Python／PySide6 延續開發，保留既有風格。

## 已通過

- 56 項自動測試：原專案額度／通知／CLI 搜尋、11 個指令、中文與換行保存、編輯及排序、刪除確認、空清單、取消、損壞檔案保留、原子保存失敗、連點防重複、剪貼簿變更取消、無目標複製備援。
- 本機 App Server 實際查詢成功。發布執行檔讀到 Codex 7 天額度、已用／剩餘數值、台灣重置時間。
- 帳號未登入與網路失敗以模擬服務回應測試；確認背景查詢程序會停止，畫面保留上次成功數值。
- 未知／缺少部分額度不顯示成 0%；其他模型不會被當成主要 Codex 額度。
- Qt 原生元件渲染：100%、125%、150% 縮放參數，含主畫面、更多、Claude、編輯、錯誤與空白。150% 空間較小時使用捲軸。
- 小螢幕可捲動、移除螢幕後座標修正的演算法已測試。編輯欄位與按鈕具有可辨識焦點。
- 單一執行個體：第二次啟動結束碼 0，原有執行個體仍在。單檔包的啟動器與主程序共兩個程序屬正常，沒有第二個主介面。
- PyInstaller Windows x64 免安裝執行檔啟動及截圖成功，結束碼 0。
- 獨立審查代理覆核程式與截圖；外框透明、明細按鈕過大、打包漏跑新測試與安裝失敗回傳已修正。

打包期間發現 PATH 中 Poppler 的 icuuc.dll 被誤收入，與 Windows 原生 ICU 匯出不相容。以依賴匯出及隔離執行確認根因後，打包腳本固定原生 DLL 搜尋路徑；重建包已成功開啟。

## 尚需實機確認

Windows 自動化的滑鼠／鍵盤回傳「GetCursorPos 存取被拒，0x80070005」，因此下列項目沒有宣稱通過：

- 在記事本、瀏覽器與 Codex 輸入框的實際中文、多行貼上與焦點返回。
- Windows 系統實際切換 100／125／150% DPI、多螢幕插拔和移動。
- 系統匣、收合及完整鍵盤操作的實際桌面驗收。

按鍵單元測試攔截 Win32 呼叫，確認僅 Ctrl＋V 的按下／放開，沒有 Enter；這不等同上述跨程式驗收。截圖由 Qt 原生元件及打包執行檔產生，未以網頁測試取代。

## 快速手動驗收

先開空白記事本，點輸入框，再點任一指令。確認中文／換行完整、焦點回到記事本，且未自動送出。於瀏覽器空白文字框、Codex 輸入框重複；再修改指令、關閉並重新開啟確認保存。若 Windows 擋住貼上，確認剪貼簿內容可用 Ctrl＋V 貼出。

## 本次介面修訂

依使用者截圖移除明細按鈕、固定操作提示列，Claude Code 恢復原專案直接顯示的卡片，常用指令置於其下方。56 項既有測試通過；重新檢查 100% 與 150% Qt 渲染，並重新打包執行檔。

## 審查修正（1.3.1）

- 61 項自動測試通過，新增儲存失敗保留草稿、重試成功、鍵盤選取、排序邊界停用、未登入／未找到／斷線徽章與小視窗操作可見性。
- 原生 Windows Qt 互動驗證通過：從編輯按鈕進入模態視窗、初始清單焦點、Tab／Shift＋Tab 保持在對話框、Esc 關閉並返回編輯按鈕。
- 同一個「新增」按鈕在聚焦前後有可量測的像素差異。清單改為整列選取及焦點，移除文字內的白色框。
- 實際走編輯按鈕與模態保存流程，模擬第一次檔案鎖定：視窗、草稿與順序保留；第二次保存成功才關閉，主畫面與重讀 JSON 均符合新內容。
- 100%、125%、150% Qt 縮放渲染完成。150% 下 8 個指令和底部操作可見，額度區可捲到完整 Claude 卡片；沒有巢狀捲動。這些是 Qt 縮放參數，並非 Windows 設定頁實機切換。
- 獨立代理依 UI UX Pro Max 與 Agentic Design System 覆核最新程式及畫面，另行執行 6 項隔離測試通過；結論 satisfied，無阻擋交付的重大 UI 問題，Utility 評分 8.3／10。小視窗換行與設計文件也已補正。

重現指令：`python tools/verify_review_fixes.py evidence/審查修正互動`。完整系統滑鼠、跨程式貼上和實機螢幕切換限制仍如上所述，不列入本次通過範圍。

## 整體縮放與欄位右側修正（1.3.2）

- 71 項自動測試通過，包含縮放值驗證、保存／取消、比例不重複累乘、僅變更比例才重啟，以及點擊箭頭開啟下拉選單與鍵盤選取。
- 設定新增 75%、90%、100%、110%、125%、150%；套用後重新開啟並記住比例。由 Qt 原生縮放處理文字、按鈕、圓環與對話框，不只改變主窗尺寸。
- 75% 與 150% 實際渲染量測：主窗寬度 300／600px、指令按鈕高度 31.5／63px、字體高度 12.75／25.5px，均成比例變化。
- 原始碼模式走過模態設定、保存 125%、退出及啟動真實新程序；新程序截圖寬度 500px，僅重啟一次。此測試使用隔離的展示模式，沒有驗證單一執行個體服務交接。
- Windows x64 單檔包分別使用已保存的 75%／150% 啟動並截圖，退出碼均為 0，畫面寬度分別為 300／600px。
- 另檢查環境 150% 加介面 150% 的合成 225%：設定選單、儲存／取消保持可見，編輯器符合工作區大小；內容過多時可捲動。這是 Qt 參數，並非 Windows 設定頁實機切換。
- 下拉欄位箭頭改用向量繪製，保留原生點擊及鍵盤操作；右側背景透明、外框連續，已比對實際截圖。
- 獨立代理依 UI UX Pro Max／Agentic Design System 覆核並執行 9 項新增測試，全部通過，結論 satisfied，無重大問題。單檔執行包的完整縮放重啟交接僅完成程式覆核，未冒充實機驗證。

重現方式：`python tools/capture_scale.py 150 evidence/整體縮放150`、`python tools/verify_scale_restart.py evidence/縮放重啟`。

## 卡片下方空白修正（1.3.3）

- 72 項自動測試通過。新增檢查較短的 Claude 卡片能縮小視窗、保留底部位置，且重新更新／展開不會恢復多餘空白；內容增加時仍可成長到高度上限。
- 高度改以卡片實際邊界量測。Windows Qt 正常額度、Claude 未安裝及錯誤狀態的卡片至常用指令標題間距均為 8px 邏輯像素；未安裝展示畫面由原本 940px 高縮為 866px。
- 工作區較小時仍限制高度並保留捲動；內容收緊不會切換整體縮放比例。重現方式：`python tools/capture_scale.py 100 evidence/空白修正 --claude-unavailable`。
